# Web Application Dependencies (required for FastAPI app)
fastapi>=0.104.0       # Modern web framework for the dashboard
uvicorn>=0.24.0        # ASGI server for FastAPI
requests>=2.31.0       # HTTP library for ThingSpeak API calls
python-dotenv>=1.0.0   # For loading environment variables from .env file
jinja2>=3.1.0          # Template engine for HTML rendering
